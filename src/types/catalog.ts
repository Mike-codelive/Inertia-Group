export type CatalogItem = {
  id: string;
  name: string;
  category: string;
  image: string;
  description: string;
  cavities: number;
  productFamily: string;
  terminalSize: number;
  sealable: boolean;
};
